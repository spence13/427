<<<<<<< HEAD
#ifndef ALIENS_H_
#define ALIENS_H_
#include <stdint.h>

int8_t aliens_hit_bottom(unsigned int *); //Check if the alien frame hits the bottom of the bunkers
int8_t aliens_all_dead(); //Check if all the aliens are dead
void erase_alien_explosion(unsigned int *); //Erase the previous alien explosion
void update_alien_pos(unsigned int *); // Wait for a char from UART, then call the appropriate functions
uint8_t kill_alien(unsigned int *, uint16_t, uint16_t); // Kill alien at the given integer
void fire_alien_bullet(unsigned int *); // Fire a bullet from a random alien
uint8_t update_alien_bullets(unsigned int *); // Advance alien bullets one frame

#endif /* ALIENS_H_ */
=======
#ifndef ALIENS_H_
#define ALIENS_H_
#include <stdint.h>

int8_t aliens_hit_bottom(unsigned int *); //Check if the alien frame hits the bottom of the bunkers
int8_t aliens_all_dead(); //Check if all the aliens are dead
void erase_alien_explosion(unsigned int *); //Erase the previous alien explosion
void update_alien_pos(unsigned int *); // Wait for a char from UART, then call the appropriate functions
uint8_t kill_alien(unsigned int *, uint16_t, uint16_t); // Kill alien at the given integer
void fire_alien_bullet(unsigned int *); // Fire a bullet from a random alien
uint8_t update_alien_bullets(unsigned int *); // Advance alien bullets one frame

#endif /* ALIENS_H_ */
>>>>>>> 46133ae2d6a022a717e89ee90e8959352f12048c
